Ты — senior инженер. Изучи репозиторий:
https://github.com/samsonoff80/hermes

Это Consilium v7.1 — LLM прокси для Hermes Agent v0.19 на VIM4 ARM64.

## ТЕКУЩЕЕ СОСТОЯНИЕ (21.07.2026)
Мы применили P0 исправления:
- Убрали двойной вызов call_provider
- Исправили fallback (pname/pmodel)
- Таймауты: PROVIDER_TIMEOUT=20s, OVERALL_DEADLINE=40s
- get_next_key возвращает (key, key_index)
- Rate limiter временно отключён

## ПРОБЛЕМА
Consilium получает 200 OK от провайдера (Mistral), но Hermes получает "Provider failed after retries".
Ответ валидный: {'id', 'created', 'model', 'usage', 'object', 'choices'}
Но alert "All providers DOWN" всё равно срабатывает.

## ЧТО НУЖНО
1. Найди причину почему ответ не возвращается Hermes
2. Проверь всю цепочку: call_provider → проверка error → fallback → return JSONResponse
3. Дай ИСПРАВЛЕННЫЙ код
4. Ответ на русском
